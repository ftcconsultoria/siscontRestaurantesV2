CREATE TABLE public."ESTQ_PRODUTO_FOTO" (
  "EPRO_FOTO_PK" bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  "EPRO_PK" bigint NOT NULL REFERENCES public."ESTQ_PRODUTO"("EPRO_PK") ON DELETE CASCADE,
  "EPRO_FOTO_URL" text,
  "EPRO_FOTO_ENVIADA" boolean DEFAULT false
);
